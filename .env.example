# Mapbox API Token (Optional)
# Get your free token at: https://account.mapbox.com/access-tokens/
# Free tier includes 50,000 tile requests per month
# If not provided, the app will use procedural textures instead

VITE_MAPBOX_TOKEN=your_mapbox_token_here
